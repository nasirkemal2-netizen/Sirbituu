name: kumkummee
description: AI Music Studio for Afaan Oromoo
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  record: ^4.2.0
  audio_waveforms: ^3.1.1
  lottie: ^2.7.0
  http: ^1.1.0
  provider: ^6.1.1
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  record: ^4.2.0
  audio_waveforms: ^3.1.1  # Add this
  lottie: ^2.7.0
  http: ^1.1.0
  provider: ^6.1.1
  audioplayers: ^5.3.0  # Add this
  permission_handler: ^10.4.4  # Add this 
dependencies:
  flutter:
    sdk: flutter
  # ... existing dependencies
  share_plus: ^7.2.0  # ADD THIS
